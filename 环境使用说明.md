# 扣子视频生成器 - 环境使用说明

本项目现已支持项目内虚拟环境，通过以下步骤可以快速部署和使用环境，无需在系统全局安装依赖。

## 部署环境

### 第一次使用
1. 双击运行 `部署环境.exe`
2. 程序会自动：
   - 创建项目内虚拟环境（位于 `.venv` 目录）
   - 安装所有必要的Python依赖包到虚拟环境
   - 创建运行脚本 `run_project.bat`
   - 更新 `生成.bat` 脚本以使用虚拟环境

### 注意事项
- 虚拟环境仅存在于项目目录内，不会影响系统全局环境
- `.venv` 目录已添加到 `.gitignore`，不会被Git跟踪
- 依赖包安装完成后，后续使用无需再次安装

## 使用项目

### 方法一：使用运行脚本（推荐）
1. 双击运行 `run_project.bat`
2. 脚本会自动：
   - 检查虚拟环境是否存在
   - 列出当前目录下的所有Excel文件
   - 提示您选择要处理的文件（或使用默认文件）
   - 使用虚拟环境中的Python运行视频生成器

### 方法二：使用命令行
```bash
.venv\Scripts\python.exe kouzi_video_generator.py [Excel文件路径]
```

### 方法三：使用生成.bat
如果您之前使用过 `生成.bat`，它已经被更新为使用虚拟环境：
```bash
双击运行 生成.bat
```

## 常见问题

### 虚拟环境丢失或损坏
如果 `.venv` 目录被删除或损坏，请重新运行 `部署环境.exe` 重新创建环境。

### 依赖包更新
如需更新依赖包，请修改 `requirements.txt` 文件后重新运行 `部署环境.exe`。

### Git协作
当其他成员通过Git克隆项目后：
1. 运行 `部署环境.exe` 创建自己的虚拟环境
2. 由于 `.venv` 被忽略，每个人的环境独立且不会相互影响

## 优势
- **环境隔离**：项目使用独立虚拟环境，不影响系统Python
- **开箱即用**：新成员克隆项目后只需运行一次部署程序
- **避免冲突**：不同项目的依赖不会相互干扰
- **版本控制**：通过 `.gitignore` 避免跟踪大型虚拟环境文件